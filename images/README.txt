Drop these image files here to replace the placeholders on the site:

  profile.jpg   -> your headshot (square works best, e.g. 600x600).
                   Shown on index.html and cloud-storage.html.

  cart-v1.png   -> Smart Cart concept render, version 1 (smart-cart.html)
  cart-v2.png   -> Smart Cart concept render, version 2 (smart-cart.html)

If a file is missing, the page shows a graceful placeholder instead of a
broken image. File names must match exactly (lowercase).
